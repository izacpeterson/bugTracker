{
  "scripts": {
    "prestart": "npm install && cd app && npm install && npm run build",
    "start": "node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "sqlite3": "^5.1.2",
    "uuid": "^9.0.0"
  }
}
